<h1> All Books List</h1>
<h3> Click On Book Title For More Info </h3>
<table border="1">
  <tr>
    <th>
      Book Title
    </th>

    <th>
    Cast Vote For Book
    </th>

    <th>
      Overall Score
    </th>
  </tr>

      <% @books.each do |book| %>
    <tr>
      <td>
        <%= link_to "#{book.title}", "/books/#{book.id}", method: :get %>
      </td>

      <td>
        <span style="float:left; margin:5px">
          <%= button_to "   👍+1   ", "/upvote/#{book.id}", method: :patch %>
        </span>
        <span style="float:right; margin:5px">
          <%= button_to "   👎-1   ", "/downvote/#{book.id}", method: :patch %>
        </span>
      </td>

      <td>
        <center>
          <%= book.vote %>
        </center>
      </td>

      </tr>
  <% end %>
</table>
<br>

<%= button_to "Add new Book", "/books/new", method: :get %>
