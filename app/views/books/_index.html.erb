<div class="container-fluid">
  <h1><%= link_to "All Books List", books_path %></h1>
  <table class="table table-hover table-condensed">
    <tr>
      <th>
        Book Title
      </th>

      <th>
      Cast Vote
      </th>

      <th>
        Overall Score
      </th>
    </tr>

    <% @books.each do |book| %>
    <tr>
      <td>
        <%= link_to "#{book.title}", book_path(book.id), method: :get %>
      </td>

      <td>
        <span style="float:left; margin:5px">
          <%= button_to "   👍+1   ", "books/upvote/#{book.id}", method: :patch %>
        </span>
        <span style="float:left; margin:5px">
          <%= button_to "   👎-1   ", "books/downvote/#{book.id}", method: :patch %>
        </span>
      </td>

      <td>
        <center>
          <%= book.vote %>
        </center>
      </td>

      </tr>
    <% end %>
  </table>
  <br>

  <%= button_to "Add new Book", new_book_path, method: :get %>
</div>
