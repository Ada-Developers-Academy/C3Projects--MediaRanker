<ol class="breadcrumb">
  <%= render partial: "all_media", locals: { active: false } %>
  <%= render partial: "category", locals: { active: false, name: "All #{ plural_display_name(@category) }" } %>
  <li class="active">New</li>
</ol>

<section class="new-edit-form">
  <%= render partial: "form", locals: { message: "Create a New #{ display_name(@category) }", method: :post } %>
</section>
