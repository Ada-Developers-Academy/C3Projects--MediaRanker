<table class="table">
  <th>Ranking</th>
  <th>Name</th>
  <th>Upvote</th>
  <% media.each do |medium| %>
    <tr>
      <td>Ranked: <%= medium.ranking %></td>
      <td><%= link_to medium.name, medium_path(medium.id) %></td>
      <td><%= link_to 'Upvote', upvote_medium_path(medium.id), class: "btn btn-default", method: "patch" %></td>
    </tr>
  <% end %>
</table>

<nav>
<%= link_to 'View All Media', media_path, method: "get", :class => "btn btn-default" %>
<%= link_to 'Add a ' + type, link_path, method: "get", :class => "btn btn-default" %>
</nav>
