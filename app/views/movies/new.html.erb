<%= render "shared/header" %>
<%= form_for @movie do |f| %>

    <%= f.label :name %>
    <%= f.text_area :name %>

    <%= f.label :director %>
    <%= f.text_area :director %>

    <%= f.label :description%>
    <%= f.text_area :description %>

    <%= f.submit 'Save' %>
    
<% end %>
