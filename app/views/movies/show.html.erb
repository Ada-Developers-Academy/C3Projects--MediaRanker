<%= render partial: 'shared/show', locals: { medium: @movie,
                                             created_alias: "Directed" } %>

<%= button_to "Upvote", upvote_movie_path(@movie), { method: :put } %>

<%= link_to "Edit #{@movie.title}", edit_movie_path(@movie) %>

<%= button_to "DELETE", @movie, { method: :delete } %>

<%= link_to "View All Movies", movies_path %>

<%= link_to "View All Media", root_path %>