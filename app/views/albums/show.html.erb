<%= render partial: "shared/show", locals: { medium: @album,
                                                    creator: @album.artist,
                                                    created_by: "Recorded by: " }%>

<%= button_to "Upvote", album_path, method: :put %>
<%= link_to "Edit #{@album.title}", edit_album_path %>
<%= button_to "Delete", album_path, method: :delete, data: { confirm: "Are you sure?"} %>
<%= link_to "View all Albums", albums_path %>
<%= link_to "View all Media", root_path %>
