<div class="row">
  <div class="col-lg-12">
    <table class="table table-hover">
      <tbody>

        <%= render partial: "shared/list_headers" %>

        <%= render partial: "shared/list", collection: @albums, as: :object,
          locals: { object_type: "albums" } %>

      </tbody>
    </table>

    <%= render partial: "shared/list_footers", locals: { object_type: "Album", new_route: new_album_path }%>
  </div>
</div>
