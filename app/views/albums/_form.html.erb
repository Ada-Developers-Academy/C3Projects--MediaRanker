<%= form_for @album do |f| %>
  <%= f.label :title, "Name" %>
  <%= f.text_field :title %>

  <%= f.label :artist %>
  <%= f.text_field :artist %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.submit "Save" %>
<% end %>
