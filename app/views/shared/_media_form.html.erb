<%= form_for(media) do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title, required: :true %>

  <%= f.label artist %>
  <%= f.text_field artist %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.submit %>

<% end %>
