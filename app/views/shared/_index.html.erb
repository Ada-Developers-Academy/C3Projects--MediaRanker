<table class="table table-hover">
  <tbody>
    <tr>
      <th>Ranking</th>
      <th>Name</th>
      <th>Upvote</th>
    </tr>
    <% collection.each do |media| %>
      <tr>
        <td> Ranked: <%= media.ranking %> </td>
        <td> <%= link_to media.name, media_path.call(media) %> </td>
        <td> <%= link_to "Upvote", media_path.call(media), method: "patch", class: "btn btn-primary" %> </td>
      </tr>
    <% end %>
  </tbody>
</table>


<%= link_to "View All Media", root_path, class: "btn btn-default" %>
<%= link_to "Add a #{media_name}", new_media_path.call, class: "btn btn-default" %>
