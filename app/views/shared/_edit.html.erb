<%= form_for subject, url: path do |f| %>
   <legend>Edit<%= "#{subject.class}" %></legend>
   <%= f.label :name %>
   <%= f.text_field :name %>

   <%= f.label by %>
   <%= f.text_field by %>

   <%= f.label :description %>
   <%= f.text_field :description %>

 <%= f.submit 'Save' %>
<% end %>
