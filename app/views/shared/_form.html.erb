<div class="row">
  <div class="col-md-12">
    <h2><%= "#{form_name}" %></h2>
      <%= form_for medium, html: { class: "form" } do |f| %>
        <div class="form-group">
          <%= f.label :title %>
          <%= f.text_field :title %>
        </div>
        <div class="form-group">
          <%= f.label created_by, "#{creator}" %>
          <%= f.text_field created_by %>
        </div>
        <div class="form-group">
          <%= f.label :description %>
          <%= f.text_area :description %>
        </div>
      <%= f.submit "Save", class: "btn btn-default"%>
    <% end %>
  </div>
</div>
