<%= form_for media  do |f| %>
  <fieldset>
    <%= f.label :name %>
    <%= f.text_field :name, value: media.name, :required => true %>

    <%= f.label :director %>
    <%= f.text_field :director, value: media.director  %>

    <%= f.label :description %>
    <%= f.text_area :description, value: media.description  %>

  <section>
    <%= f.submit "Save" %>
  </section>
<% end %>
