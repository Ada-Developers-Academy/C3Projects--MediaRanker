<%= form_for object, method: http_verb do |f| %>

  <fieldset>
    <% details.each do |detail| %>
      <%= f.label detail %>
      <%= f.text_field detail %>
    <% end %>
  </fieldset>

  <%= f.submit "Save" %>

<% end %>
