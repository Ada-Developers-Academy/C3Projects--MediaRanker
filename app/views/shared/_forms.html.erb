<%= form_for object do |f| %>

  <fieldset>
    <% details.each do |detail| %>
      <%= f.label detail %>
      <%= f.text_field detail %>
    <% end %>
  </fieldset>

  <%= f.submit "Save" %>

<% end %>
